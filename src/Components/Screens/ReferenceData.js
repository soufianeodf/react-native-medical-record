import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ReferenceData = () => {
  return (
    <View>
      <Text>welcome to ReferenceData page</Text>
    </View>
  );
}

export default ReferenceData;

const styles = StyleSheet.create({});
